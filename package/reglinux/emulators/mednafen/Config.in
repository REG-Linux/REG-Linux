config BR2_PACKAGE_MEDNAFEN
    bool "mednafen"
    select BR2_PACKAGE_SDL2
    select BR2_PACKAGE_ZLIB
    select BR2_PACKAGE_LIBPNG
    select BR2_PACKAGE_FLAC

    help
        Mednafen is a portable, utilizing OpenGL and SDL, argument(command-line)-driven multi-system emulator.

if BR2_PACKAGE_MEDNAFEN

config BR2_PACKAGE_MEDNAFEN_PCFX
        bool "mednafen-pcfx"

config BR2_PACKAGE_MEDNAFEN_PSX
        bool "mednafen-psx"

config BR2_PACKAGE_MEDNAFEN_SATURN
        bool "mednafen-saturn"

endif
