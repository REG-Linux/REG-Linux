# rg552 default values

# the button can be pressed by mistake
system.shutdowndelay=1

# default suspend on handled boards
system.suspendmode=suspend

# default wayland environment (rotate)
system.es.environment=wayland

# default card
audio.device=alsa_output._sys_devices_platform_es8316-sound_sound_card0.HiFi__Speaker__sink
