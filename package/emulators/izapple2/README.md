# izapple2

Apple II emulator written in Go. Upstream: https://github.com/ivanizag/izapple2

Uses `golang-package`. The SDL2 frontend (`frontend/a2sdl`) is built separately and installed as `/usr/bin/izapple2`.
