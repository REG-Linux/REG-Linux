config BR2_PACKAGE_LIBRETRO_MORPHEUSCAST_XTREME
        bool "libretro-morpheuscast-xtreme"
	
	depends on BR2_INSTALL_LIBSTDCPP
	depends on BR2_PACKAGE_HAS_LIBGLES
        help
          A libretro dreamcast emulator core.

	  http://www.libretro.com

comment "LIBRETRO_MORPHEUSCAST_XTREME needs a toolchain w/ C++ and OpenGL ES or OpenGL"
	depends on !BR2_INSTALL_LIBSTDCPP || !BR2_PACKAGE_HAS_LIBGLES
