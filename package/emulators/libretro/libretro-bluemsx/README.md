# libretro-bluemsx

MSX / ColecoVision libretro core (BlueMSX). Upstream: https://github.com/libretro/blueMSX-libretro

Platform string maps Raspberry Pi models to specific `rpi*` targets; other AArch64 boards fall back to `unix`.
