# libretro-hatari

Atari ST / STE / TT libretro core (Hatari). Upstream: https://github.com/libretro/hatari

Requires `libcapsimage` and `zlib`; platform string maps RPi models to `rpi*` targets and S812 to `armv`.
