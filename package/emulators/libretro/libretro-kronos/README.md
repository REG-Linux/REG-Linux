# libretro-kronos

Sega Saturn libretro core (Kronos). Upstream: https://github.com/FCare/kronos

GLES targets pass `FORCE_GLES=1`; platform uses SoC-specific strings (e.g. `odroid`, `odroid-n2`, `rockpro64`, `odroid-c4`, `arm64`).
