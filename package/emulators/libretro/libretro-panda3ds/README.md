# libretro-panda3ds

Nintendo 3DS libretro core (Panda3DS). Upstream: https://github.com/REG-Linux/Panda3DS

Hosted on the REG-Linux fork. Vulkan support requires `glslang` + `host-glslang`; a patch removes GLX from the GLAD loader for EGL-only builds.
