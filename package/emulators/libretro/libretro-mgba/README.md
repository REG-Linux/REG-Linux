# libretro-mgba

Game Boy Advance libretro core (mGBA). Upstream: https://github.com/mgba-emu/mgba

Uses `cmake-package` with GLES3/GLES2 renderer selection (`-DBUILD_GLES3=ON` vs `-DBUILD_GLES2=ON`). Qt, SDL, debugger, and Discord RPC are all disabled.
