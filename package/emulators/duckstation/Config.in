config BR2_PACKAGE_DUCKSTATION
    bool "duckstation"
    depends on BR2_x86_64 || BR2_arm || BR2_aarch64
    # TODO depends on what else ? Qt ? GL ? Vulkan ?

    help
      DuckStation - PlayStation 1, aka. PSX Emulator
      DuckStation features a fully-featured frontend built using Qt.

	    https://github.com/stenzek/duckstation
