# supermodel

Sega Model 3 arcade emulator. Upstream: https://github.com/trzy/Supermodel

Uses the upstream `Makefiles/Makefile.UNIX` directly, with `CC`, `CXX`, and `sdl2-config` path-substituted for cross-compilation. Requires desktop GL.
