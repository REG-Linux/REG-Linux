# BR2_PACKAGE_BOX64

Box64 https://github.com/ptitseb/box64

## Build notes

- ``Version``: v0.3.8
- ``Config``: depends on BR2_aarch64 || BR2_RISCV_64, depends on BR2_PACKAGE_HOST_PYTHON3
- ``Build helper``: CMake-based (cmake-package)
