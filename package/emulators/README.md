# emulators

Standalone emulator packages. Each subdirectory contains a `Config.in`, a `.mk` recipe, patches, and any supporting assets (evmapy `.keys` files, default configs, data files). The `libretro/` subtree holds libretro cores built as `.so` plugins for the RetroArch-compatible frontend.
