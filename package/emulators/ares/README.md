# ares

Multi-system emulator. Upstream: https://github.com/ares-emulator/ares

Requires `host-ares` built with `-DARES_BUILD_SOURCERY_ONLY=ON` to generate the `sourcery` code-generation tool used during cross-compilation. Depends on librashader and xwayland.
