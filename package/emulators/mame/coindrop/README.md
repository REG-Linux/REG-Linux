# coindrop

MAME plugin that plays a coin-drop sound effect when a coin is inserted. Upstream: https://github.com/DirtBagXon/coindrop

Bundled as a MAME plugin directory; the build copies it verbatim into `/usr/bin/mame/plugins/coindrop/` during the MAME install step.
