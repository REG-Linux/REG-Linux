# openmsx

MSX emulator. Upstream: https://github.com/openMSX/openMSX

Uses the upstream GNU Makefile (not autotools despite the `autotools-package` wrapper). `libglew` is added only when desktop GL is present.
