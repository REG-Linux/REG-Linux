# hatari

Atari ST/STe/TT/Falcon emulator. Upstream: https://github.com/hatari/hatari

`CAPSIMAGE_INCLUDE_DIR` is explicitly pointed at `$(STAGING_DIR)/usr/include` to locate the IPF disk image library.
