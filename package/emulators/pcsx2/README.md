# pcsx2

PlayStation 2 emulator. Upstream: https://github.com/PCSX2/pcsx2

x86_64 only. Uses clang when available (`-DCMAKE_C_COMPILER=clang`). OpenGL and Vulkan enabled conditionally. Requires Qt6 and X11/Wayland.
