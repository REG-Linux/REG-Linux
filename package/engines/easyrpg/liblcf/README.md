# liblcf

RPG Maker 2000/2003 data library from the EasyRPG project. Upstream: https://github.com/EasyRPG/liblcf

Staging install is enabled (`LIBLCF_INSTALL_STAGING = YES`) so downstream packages (EasyRPG Player, libretro-easyrpg) can find headers and libraries during their CMake configure step.
