# OpenBOR 7530

DCurrent OpenBOR 4.x branch (`v7533`). Upstream: https://github.com/DCurrent/openbor

Uses `BUILD_LINUX_LE_x86_64`, `BUILD_LINUX`, or `BUILD_LINUX_LE_arm` flags depending on the target architecture. Installs as `/usr/bin/OpenBOR7530`.
