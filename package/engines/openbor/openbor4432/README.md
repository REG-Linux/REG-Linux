# OpenBOR 4432

Legacy `v3b4432` fork of OpenBOR. Upstream: https://github.com/Darknior/OpenBORv3b4432

On RISC-V the build falls back to the x86_64 `BUILD_PLATFORM` flag because there is no dedicated RISC-V path in this fork's Makefile. Installs as `/usr/bin/OpenBOR4432`.
