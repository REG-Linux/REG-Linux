# sonic3-air

Sonic 3 A.I.R. engine (Oxygen Engine). Upstream: https://github.com/Eukaryot/sonic3air

When `BR2_PACKAGE_XORG7` is set the standard `_cmake` subdir is used; otherwise a custom `CMakeLists.txt.gles2` copied into `_gles2/` is used for GLES2-only targets.
