# RetroGame

Raspberry Pi GPIO-to-USB utility. Upstream: https://github.com/adafruit/Adafruit-Retrogame

The source is bundled in-tree (no upstream download); the build copies files from the package directory and compiles with the cross-compiler directly.
