# batocera/core

Core infrastructure packages inherited from the Batocera lineage. Each subdirectory provides a Buildroot recipe plus init scripts under `/etc/init.d`. Packages here cover audio (PipeWire/ALSA wiring), controller overlays, udev rules, and triggerhappy hotkey bindings.
