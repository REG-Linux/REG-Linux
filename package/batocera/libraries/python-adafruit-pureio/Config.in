config BR2_PACKAGE_PYTHON_ADAFRUIT_PUREIO
	bool "python-adafruit-pureio"
	select BR2_PACKAGE_HOST_PYTHON_SETUPTOOLS_SCM
	help
	  Pure python (i.e. no native extensions) access to Linux IO including
	  I2C and SPI. Drop in replacement for smbus and spidev modules.

	  https://pypi.org/project/Adafruit-PureIO
