# xash3d

Xash3D-FWGS engine and Half-Life SDK game libraries for GoldSource mod support. The engine package and three separate HLSDK forks (base HL, Deathmatch Classic, Opposing Force) each install their `.so` files under `/usr/lib/xash3d/hlsdk/<game>/`.
