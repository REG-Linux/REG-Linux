# ports

PC/console engine ports and source-code remakes. Each subdirectory is a self-contained Buildroot package. Most install an evmapy `.keys` file to `/usr/share/evmapy/` for controller mapping and a data-init directory under `/usr/share/reglinux/datainit/` when a default config is needed.
