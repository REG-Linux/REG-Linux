# GemRB

Infinity Engine (Baldur's Gate, Icewind Dale, Planescape: Torment) reimplementation. Upstream: https://github.com/gemrb/gemrb

`-DOPENGL_BACKEND` is set to `"OpenGL"` or `"GLES"` depending on what the target provides. The bundled demo data is removed from the install. LibVLC is disabled.
