# cannonball

OutRun engine reimplementation (Cannonball SE). Upstream: https://github.com/J1mbo/cannonball-se

Target is `sdl2gl` on x86_64 and `sdl2gles` everywhere else. ROM and config paths are hardwired under `/userdata`.
