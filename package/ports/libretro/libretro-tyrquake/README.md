# libretro-tyrquake

Quake I libretro core (TyrQuake). Upstream: https://github.com/libretro/tyrquake

Platform string ranges from `armv` to `rpi5_64` or `unix`; `GIT_VERSION` is stamped at build time.
