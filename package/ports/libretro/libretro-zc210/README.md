# libretro-zc210

Zelda Classic libretro core. Upstream: https://github.com/netux79/zc210-libretro

Built with `platform="unix"` and a truncated `GIT_VERSION` stamp. Installs `zc210_libretro.so` to `/usr/lib/libretro/`.
