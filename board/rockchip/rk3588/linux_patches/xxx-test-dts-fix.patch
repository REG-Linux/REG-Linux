--- a/arch/arm64/boot/dts/rockchip/rk3588s-gameforce-ace.dts	2025-11-28 12:25:51.404713657 +0100
+++ b/arch/arm64/boot/dts/rockchip/rk3588s-gameforce-ace.dts	2025-11-28 12:29:44.892244042 +0100
@@ -457,11 +459,19 @@
 	cpu-supply = <&vdd_cpu_big1_s0>;
 };
 
+/* ------------------------------------------------------------------------- */
+/*                           MIPI DSI + PANEL FIX                            */
+/* ------------------------------------------------------------------------- */
+
 &dsi0 {
 	#address-cells = <1>;
 	#size-cells = <0>;
 	status = "okay";
 
+	/* FIX: mandatory for MIPI PHY */
+	phys = <&mipidcphy0>;
+	phy-names = "mipi";
+
 	panel@0 {
 		compatible = "huiling,hl055fhav028c", "himax,hx8399c";
 		reg = <0>;
@@ -608,6 +618,10 @@
 	};
 };
 
+/* ------------------------------------------------------------------------- */
+/*                     BATTERY GAUGE cw2015 POWER FIX                        */
+/* ------------------------------------------------------------------------- */
+
 &i2c6 {
 	pinctrl-0 = <&i2c6m3_xfer>;
 	status = "okay";
@@ -624,11 +638,14 @@
 		wakeup-source;
 	};
 
-	/* Battery profile from BSP device tree. */
 	battery@62 {
 		compatible = "cellwise,cw2015";
 		reg = <0x62>;
 
+		/* FIX: required to avoid -19 ENODEV */
+		vbat-supply = <&vcc_3v3_s3>;
+		vdd-supply  = <&vcc_3v3_s3>;
+
 		cellwise,battery-profile = /bits/ 8
 			<0x18 0x0A 0x76 0x6A 0x6A 0x6A 0x68 0x66
 			 0x62 0x5E 0x5A 0x58 0x5F 0x59 0x46 0x3D
@@ -1263,26 +1280,32 @@
 	status = "okay";
 };
 
-&uart2 {
-	pinctrl-0 = <&uart2m0_xfer>;
-	status = "okay";
-};
+/* ------------------------------------------------------------------------- */
+/*                    BLUETOOTH FIX — MOVE TO UART2                          */
+/* ------------------------------------------------------------------------- */
 
 &uart9 {
-	pinctrl-0 = <&uart9m2_xfer>, <&uart9m2_ctsn>, <&uart9m2_rtsn>;
-	uart-has-rtscts;
+	status = "disabled";  /* FIX: disable wrong port */
+};
+
+&uart2 {
+	pinctrl-0 = <&uart2m0_xfer>;
 	status = "okay";
 
 	bluetooth {
 		compatible = "brcm,bcm4345c5";
+		max-speed = <3000000>;
+
+		/* validated pin mapping */
+		shutdown-gpios = <&gpio3 RK_PB7 GPIO_ACTIVE_HIGH>;
+		device-wakeup-gpios = <&gpio3 RK_PB1 GPIO_ACTIVE_HIGH>;
+		host-wake-gpios = <&gpio3 RK_PB0 GPIO_ACTIVE_LOW>;
+
 		clocks = <&rtc_hym8563>;
 		clock-names = "lpo";
-		device-wakeup-gpios = <&gpio3 RK_PB1 GPIO_ACTIVE_HIGH>;
-		interrupt-parent = <&gpio3>;
-		interrupts = <RK_PB0 IRQ_TYPE_EDGE_FALLING>;
-		pinctrl-0 = <&bt_enable_h>, <&bt_host_wake_l>, <&bt_wake_l>;
+
 		pinctrl-names = "default";
-		shutdown-gpios = <&gpio3 RK_PB7 GPIO_ACTIVE_HIGH>;
+		pinctrl-0 = <&bt_enable_h>, <&bt_wake_l>, <&bt_host_wake_l>;
 	};
 };
 
