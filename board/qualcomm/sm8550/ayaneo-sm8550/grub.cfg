set timeout_style=menu

# Text-only color scheme (firmware text console).
set menu_color_normal=light-gray/black
set menu_color_highlight=black/light-cyan
set color_normal=light-gray/black
set color_highlight=yellow/black

# Try to load previously saved entry (grubenv next to grub.cfg).
load_env

if [ -z "${saved_entry}" ]; then
    # First boot or missing env: show menu with no timeout.
    set default="0"
    set timeout="-1"
else
    # Subsequent boots: boot straight into the saved entry.
    set default="${saved_entry}"
    set timeout="3"
fi

menuentry "AYANEO Pocket S" --id=pockets {
    set saved_entry="pockets"
    save_env saved_entry
    linux /boot/linux label=REGLINUX rootwait earlycon fbcon=rotate:3 rootfstype=ramfs rw init=/init loglevel=9 clk_ignore_unused pd_ignore_unused vt.global_cursor_default=0
    initrd /boot/initrd.lz4
    devicetree /boot/sm8550-ayaneo-ps.dtb
}

menuentry "AYANEO Pocket Ace" --id=pocketace {
    set saved_entry="pocketace"
    save_env saved_entry
    linux /boot/linux label=REGLINUX rootwait earlycon fbcon=rotate:3 rootfstype=ramfs rw init=/init loglevel=9 clk_ignore_unused pd_ignore_unused vt.global_cursor_default=0
    initrd /boot/initrd.lz4
    devicetree /boot/qcs8550-ayaneo-pocketace.dtb
}

menuentry "AYANEO Pocket DMG" --id=pocketdmg {
    set saved_entry="pocketdmg"
    save_env saved_entry
    linux /boot/linux label=REGLINUX rootwait earlycon fbcon=rotate:3 rootfstype=ramfs rw init=/init loglevel=9 clk_ignore_unused pd_ignore_unused vt.global_cursor_default=0
    initrd /boot/initrd.lz4
    devicetree /boot/qcs8550-ayaneo-pocketdmg.dtb
}

menuentry "AYANEO Pocket DS" --id=pocketds {
    set saved_entry="pocketds"
    save_env saved_entry
    linux /boot/linux label=REGLINUX rootwait earlycon fbcon=rotate:3 rootfstype=ramfs rw init=/init loglevel=9 clk_ignore_unused pd_ignore_unused vt.global_cursor_default=0
    initrd /boot/initrd.lz4
    devicetree /boot/qcs8550-ayaneo-pocketds.dtb
}

menuentry "AYANEO Pocket Evo" --id=pocketevo {
    set saved_entry="pocketevo"
    save_env saved_entry
    linux /boot/linux label=REGLINUX rootwait earlycon fbcon=rotate:3 rootfstype=ramfs rw init=/init loglevel=9 clk_ignore_unused pd_ignore_unused vt.global_cursor_default=0
    initrd /boot/initrd.lz4
    devicetree /boot/qcs8550-ayaneo-pocketevo.dtb
}

menuentry "Reboot" {
    reboot
}
