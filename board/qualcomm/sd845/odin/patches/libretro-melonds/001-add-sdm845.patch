--- a/Makefile	2025-11-17 10:01:29.847668275 +0100
+++ b/Makefile	2025-11-17 14:51:35.795895907 +0100
@@ -351,6 +351,20 @@
    HAVE_THREADS = 1
    JIT_ARCH = aarch64
 
+# SDM845
+else ifeq ($(platform), sdm845)
+   EXT ?= so
+   CPUFLAGS += -mcpu=cortex-a75.cortex-a55 -mtune=cortex-a75.cortex-a55
+   HAVE_NEON = 1
+   TARGET := $(TARGET_NAME)_libretro.$(EXT)
+   fpic := -fPIC
+   SHARED := -shared -Wl,--version-script=$(CORE_DIR)/link.T -Wl,--no-undefined
+   LIBS += -lpthread
+   HAVE_OPENGLES3 = 1
+   HAVE_OPENGL = 0
+   HAVE_THREADS = 1
+   JIT_ARCH = aarch64
+
 # SM8250
 else ifeq ($(platform), sm8250)
    EXT ?= so
