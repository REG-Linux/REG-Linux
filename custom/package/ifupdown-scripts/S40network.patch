diff --git a/package/ifupdown-scripts/S40network b/package/ifupdown-scripts/S40network
index 642c5013ac..77aefb029e 100644
--- a/package/ifupdown-scripts/S40network
+++ b/package/ifupdown-scripts/S40network
@@ -1,21 +1,14 @@
-#!/bin/sh
-#
+#!/bin/dash
 # Start the network....
-#
-
-# Debian ifupdown needs the /run/network lock directory
-mkdir -p /run/network
 
 case "$1" in
   start)
 	printf "Starting network: "
-	/sbin/ifup -a
-	[ $? = 0 ] && echo "OK" || echo "FAIL"
+	{ [ /sbin/ifup -a ] && echo "OK"; } || echo "FAIL"
 	;;
   stop)
 	printf "Stopping network: "
-	/sbin/ifdown -a
-	[ $? = 0 ] && echo "OK" || echo "FAIL"
+	{ [ /sbin/ifdown -a ] && echo "OK"; } || echo "FAIL"
 	;;
   restart|reload)
 	"$0" stop
@@ -25,6 +18,3 @@ case "$1" in
 	echo "Usage: $0 {start|stop|restart}"
 	exit 1
 esac
-
-exit $?
-
